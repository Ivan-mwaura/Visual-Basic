Option Compare Text


Module Program
    Sub Main()


        ' Dim number As Integer
        'Dim x As Integer
        'Dim y As Integer
        'Dim z As Integer
        'Dim sol As Integer
        'Dim rem1 As Integer

        'Console.WriteLine("Enter a 3 digit value ")
        ' number = CInt(Console.ReadLine())

        ' x = number \ 100 
        ' rem1 = number Mod 100
        'y = rem1 \ 10
        'z = rem1 Mod 10

        'sol = z + y + x

        'Console.WriteLine("The sum is: " & sol)

        'Dim RandomNumber1 As Integer, RandomNumber2 As Integer, RandomNumber3 As Integer
        'Dim sum As Integer

        'Randomize()
        'RandomNumber1 = Int((10 * Rnd()) + 1)
        'RandomNumber2 = Int((10 * Rnd()) + 1)
        'RandomNumber3 = Int((10 * Rnd()) + 1)

        'sum = RandomNumber1 + RandomNumber2 + RandomNumber3

        'Console.WriteLine("The sum of " & RandomNumber1 & "+ " & RandomNumber2 & " + " & RandomNumber3 & " = " & sum)

        Dim name1, name2 As String

        name1 = "Jkuat"
        name2 = InputBox("Enter name2")

        If (name1 = name2) Then
            MsgBox("They are Equal")

        Else
            MsgBox("They are Equal")

        End If


    End Sub
End Module
